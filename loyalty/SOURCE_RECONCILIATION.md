# Stored source-reference reconciliation and collection — 2.7.0

## Finite scope

This pass reconciles every explicit HTTP(S) reference in the saved original manual loyalty, corporate, community, source-audit and old raw-inbox datasets, including URLs embedded in notes and native hyperlink targets. It is not recursive crawling of every link in collected JSON, PDF documents, websites or previous conversation transcripts. Public adapter records and source reports are the comparison evidence, not extra unexplored input links.

The private input snapshot was read from Google Drive on 2026-09-14 before publication. No corporate/community content, codes, row contents, spreadsheet identifier or private export is committed here. The full URL-to-cell disposition is delivered privately, not as a public Actions artifact.

There are **334 distinct explicit text URLs plus nine additional native links on company-name labels: 343 references**. Comparison lowercases scheme/host and drops fragments, while preserving scheme, path case and query. There are no unassigned dispositions in this fixed set. Disposition does not mean successful access or verified personal eligibility.

| Disposition group | References | Meaning |
|---|---:|---|
| Observed exact source or actual registered catalogue report | 104 | 101 record source URLs and three working catalogue/channel routes |
| Existing identity, alternate URL | 19 | 18 exact S7 native codes and one Utair trailing-slash path; alternate URL not independently fetched |
| Concrete failures or source-precheck blocks | 101 | 72 explicit discovery failures, 15 previously documented exact failures, seven failed registered roots, seven source-precheck failures before these individual targets |
| Private programme references | 88 | Corporate/community data remains in private normalization; a public merchant homepage cannot verify the private deal |
| Historical channel references | 14 | Read archive evidence; official channel collection is a filtered 180-day window, not complete historical coverage |
| Non-incremental/reference material | 17 | Nine automatic company-label hyperlinks, one internal workbook reference, and seven general/secondary/operational pages |

All embedded formula fields in the five input areas were inspected; they introduce no additional external URLs. Company-name auto-links are explicitly not inferred programme-benefit cards. Native-code matching is not a redirect assertion and does not copy conditions between observations.

## Newly implemented public routes

Ten exact programme-rule pages now supplement the existing Ural Wings catalogue: Ramada Ekaterinburg, Sea Galaxy, Smart, Vozdushnaya Gavan, Cars&GO, GLORIA, Golden Triangle, Vysotsky Hotel, Angara, and EcoSpa Vysotsky. Exact addresses, scoped selectors and required identity text are in `known_rules.json`; all are `program_rules` with supplementary provenance, not ten additional independent discounts.

Two official public announcement archives are added: `promomir` and `mybspb`. The latter retains only explicit YARKO/EKP/loyalty-context benefits rather than classifying bank interest rates as rewards. Both use the existing 180-day/60-page bounded traversal, source-post identity and `announced_unverified` boundary. Outgoing links are evidence, not automatically fetched cards. Publication date is not expiry and roundups do not assign every rate to a single guessed partner.

The Telegram extractor excludes reply excerpts and link previews. Nested native text wrappers now represent one message, while two disjoint text blocks still fail as ambiguous rather than being concatenated. This correction was reproduced red/green; it does not claim universal Telegram markup coverage.

## Deliberate exclusions and disagreement

The Personalguide Gorskiy directory publishes 5/10/15 while the observed official Ural table states 5/7/10. The directory was not used to overwrite the official observation. `v.page` is a mirror of FPC announcements, not another authoritative catalogue. Orgs.biz and coupon aggregators are not promoted to current primary programme evidence. The AZIMUT booking search and Mir landing-page navigation are not new offers; route-dependent excess-baggage pricing is not falsely represented as another Gold benefit.

Old S7 paths with the same case-sensitive native code are linked to existing collected records, not counted as newly discovered discounts. Unavailable pages remain unavailable; no old price, rate or code is relabelled as freshly fetched.

## Reproducible development evidence

- Discovery run `34819206189`, artifact `10337563167`: 104 bounded public URL diagnostics. ZIP SHA256 `b2fd3c4f859c3e740adcd1fa1471930b25027aa376d82ffd162799b62a93d5f3`.
- Additional-reference run `34828043571`, artifact `10339839923`: nine concrete remaining references. ZIP SHA256 `7521101e87698ecd5fc2d6ec768f03efda604f1e6ee31aea645ab9a5c31856aa`.
- Recovered route test `34829639844`, artifact `10340954949`: 47 records before the nested-text correction; failures and partial statuses retained. ZIP SHA256 `aa6e8fde6e89f12ee086861018b80ebfe58c4716b06ab0a65916b55900fe6416`.
- Final branch run `34833398557`, artifact `10343291875`: **50 records from all 12 new routes**, 11 ok/one partial. Ten rule bundles, 24 Mir announcements, 16 bank announcements. Mir has one ambiguous post (`promomir/1635`) recorded as an error; both channel walks reached the configured time boundary. ZIP SHA256 `e5a2dd1a4b1dadc156995d100f1b8cd6115b04747df1288460277613c839b1f8`.

All 50 branch records passed independent JSON Schema/application validation and publisher preparation. All 31 code-manifest entries matched the downloaded tested code. Full recovery suite: **409 Python tests and seven KEY tests passed**. An intermediate checksum-bound transfer failed on a test-file blank line before collection; the exact bytes were corrected without weakening the hash check.

## Publication and preserved boundaries

Main publication is a separate gate: check the actual main payload and completed publisher, then independently read all written parser and unified values. This branch report alone is not evidence that the spreadsheet was updated.

Temporary diagnostic workflows/scripts and one-time registration helpers are removed before integration. No changes to KEY, production workflows, WIF, Google permissions or schedule flags are required. Existing private normalization runs after authorized public publication. The original manual/source-audit/raw tabs and manual comment columns must remain unchanged. No paid service, proxy, personal session, CAPTCHA solver or TLS override was introduced.

**Closed:** reconciliation of the declared fixed stored-reference inventory and implementation of the tested incremental routes. **Not claimed:** all possible websites/regions/history/private catalogues, an exhaustive semantic interpretation of PDFs, or stable access to the eight previously blocked primary routes. A failure/precheck/private disposition remains a real coverage limit, not a disguised success.
