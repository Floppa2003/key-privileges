name: Seven public routes - DNS edges and HTTP3
on:
  push:
    branches: ["diagnostics/seven-routes-transport-20260915"]
    paths: ["diagnostics/seven_routes_edges.py", ".github/workflows/seven-routes-edges.yml"]
permissions:
  contents: read
jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-24.04, ubuntu-slim]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - run: python -m pip install requests==2.32.5 beautifulsoup4==4.14.3 curl_cffi==0.16.0
      - name: Read exact targets with controlled DNS and HTTP3
        env:
          RUNNER_LABEL: ${{ matrix.runner }}
        run: python diagnostics/seven_routes_edges.py
      - name: Preserve diagnostic evidence only
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: seven-edges-${{ matrix.runner }}-${{ github.run_id }}
          path: seven-edge-output/
          if-no-files-found: warn
          retention-days: 7
